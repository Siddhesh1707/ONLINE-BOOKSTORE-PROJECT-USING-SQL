# ONLINE-BOOKSTORE-PROJECT-USING-SQL
SQL project that analyzes an online bookstore database using three datasets (Books, Customers, Orders). The project demonstrates database design, data import from CSV files, and SQL queries including joins, aggregations, filtering, and analytical queries to generate business insights.
